{% extends "base.html.twig" %}

{% block content %}


    <div class="container">
        <div class="row">
            <div class="col-md-3 col-md-offset-4">
                {% if session is null %}
                    <a class="btn btn-block btn-social btn-facebook" href="{{ loginToFb }}"><i class="fa fa-facebook"></i>Log
                        in
                        with Facebook!</a>
                    <a class="btn btn-block btn-social btn-google" href="{{ loginToG_plus }}"> <i class="fa fa-google"></i>
                        Sign
                        in with Google</a>
                    <a class="btn btn-block btn-social btn-github"> <i class="fa fa-github"></i> Sign in with GitHub</a>
                {% else %}
                    {% include 'checkKindToEmailLogginUser.html.twig' %}
                {% endif %}
            </div>
        </div>
    </div>

    <button id="politic">Polityka</button>
    <button id="music">Muzyka</button>
    <button id="sport">Sport</button>
    <button id="anything">Ogólne</button>

    <div class="politic"></div>
    <div class="music"></div>
    <div class="sport"></div>
    <div class="anything"></div>

{% endblock %}