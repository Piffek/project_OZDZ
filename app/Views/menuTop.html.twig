
	{% if session is null %}
    	<a class="btn btn-block btn-social btn-facebook" href="{{ loginToFb }}"><i class="fa fa-facebook"></i>Log in with Facebook!</a>
    	<a class="btn btn-block btn-social btn-google" href="{{ loginToG_plus }}"> <i class="fa fa-google"></i> Sign in with Google</a>
    	<a class="btn btn-block btn-social btn-github"> <i class="fa fa-github"></i> Sign in with GitHub</a>
    {% else %}
    	Witaj {{ session }}
    	Wybierz kategorie, z której informacje chcesz otrzymywać na mail.
    	<form action="/rodzaj" method="POST">
    		<select name="kind">
    			<option value="politic">Polityka</option>
    			<option value="music">Muzyka</option>
    			<option value="sport">Sport</option>
    			<option value="anything">Ogólne</option>
    		</select>
    	<button type="submit" value="Wyslij">Dodaj</button>
    	</form>
	{% endif %}
